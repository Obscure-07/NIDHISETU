// Web implementation — uses react-native-web-maps which is web-safe.
import MapView, { Marker, PROVIDER_GOOGLE } from 'react-native-web-maps';

export default MapView;
export { Marker, PROVIDER_GOOGLE };
